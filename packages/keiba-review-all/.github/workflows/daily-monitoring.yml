name: Weekly Monitoring

on:
  schedule:
    # 毎週月曜AM9:00 JST（UTC 0:00）
    - cron: '0 0 * * 1'
  workflow_dispatch: # 手動実行も可能

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate Airtable schema
        env:
          AIRTABLE_API_KEY: ${{ secrets.AIRTABLE_API_KEY }}
          AIRTABLE_BASE_ID: ${{ secrets.AIRTABLE_BASE_ID }}
        run: node scripts/production/schema-validator.cjs
        continue-on-error: true  # スキーマエラーでも続行

      - name: Run daily monitoring
        env:
          AIRTABLE_API_KEY: ${{ secrets.AIRTABLE_API_KEY }}
          AIRTABLE_BASE_ID: ${{ secrets.AIRTABLE_BASE_ID }}
        run: node scripts/production/daily-monitoring.cjs

      - name: Summary
        if: success()
        run: |
          echo "✅ 日次モニタリングが完了しました"
          echo "📅 実行日時: $(date)"

      - name: Failure notification
        if: failure()
        run: |
          echo "❌ モニタリングに失敗しました"
          echo "📋 GitHub Actionsログを確認してください"
          echo "🔗 https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
