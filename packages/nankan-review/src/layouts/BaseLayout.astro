---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const {
  title,
  description = '南関競馬（大井・川崎・船橋・浦和）の予想サイト口コミ・評判を集約。首都圏の地方競馬ファンに最適なサイト選びをサポート。',
  ogImage = '/og/default.png',
} = Astro.props;

const siteTitle = '南関競馬予想サイト口コミ';
const fullTitle = title === 'トップ' ? siteTitle : `${title} | ${siteTitle}`;
const siteUrl = import.meta.env.SITE_URL || 'https://nankan.keiba-review.jp';
const canonicalUrl = new URL(Astro.url.pathname, siteUrl).href;
const ogImageUrl = ogImage.startsWith('http') ? ogImage : `${siteUrl}${ogImage}`;
const GA_ID = import.meta.env.PUBLIC_GA_ID;
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />

    <!-- Google Search Console Verification -->
    <meta name="google-site-verification" content="LJ1qNn3SZFuo5zHjLtI58OZSKKXXeVugmiXG2SPGMe8" />

    <!-- OGP -->
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:site_name" content={siteTitle} />
    <meta property="og:locale" content="ja_JP" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />

    <!-- Canonical -->
    <link rel="canonical" href={canonicalUrl} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Preconnect to external domains -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="dns-prefetch" href="https://fonts.googleapis.com" />

    <!-- Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&display=swap"
      rel="stylesheet"
    />

    <!-- Google Analytics 4 -->
    {GA_ID && (
      <>
        <script async src={`https://www.googletagmanager.com/gtag/js?id=${GA_ID}`}></script>
        <script is:inline define:vars={{ GA_ID }}>
          window.dataLayer = window.dataLayer || [];
          window.gtag = function gtag(){dataLayer.push(arguments);}
          gtag('js', new Date());
          gtag('config', GA_ID, {
            page_path: window.location.pathname,
          });
        </script>
      </>
    )}

    <title>{fullTitle}</title>
  </head>
  <body class="min-h-screen flex flex-col">
    <!-- Header -->
    <header class="bg-white/95 backdrop-blur-md shadow-sm sticky top-0 z-50 border-b border-slate-200">
      <div class="max-w-6xl mx-auto px-4 py-3 md:py-4">
        <div class="flex items-center justify-between">
          <a href="/" class="flex items-center gap-2 group">
            <div class="w-8 h-8 md:w-10 md:h-10 bg-gradient-to-br from-blue-700 to-sky-500 rounded-lg flex items-center justify-center shadow-lg group-hover:shadow-xl transition-shadow">
              <svg class="w-5 h-5 md:w-6 md:h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
              </svg>
            </div>
            <span class="text-lg md:text-xl font-bold bg-gradient-to-r from-blue-700 to-sky-500 bg-clip-text text-transparent transition-all">
              南関競馬予想サイト口コミ
            </span>
          </a>
          <nav class="hidden md:flex items-center gap-1 bg-slate-100 rounded-lg p-1">
            <a href="/sites/" class="px-4 py-2 text-sm font-medium text-slate-700 hover:text-blue-600 hover:bg-white rounded-md transition-all">
              サイト一覧
            </a>
            <div class="w-px h-5 bg-slate-300"></div>
            <a href="/about/" class="px-4 py-2 text-sm font-medium text-slate-700 hover:text-blue-600 hover:bg-white rounded-md transition-all">
              このサイトについて
            </a>
          </nav>
          <!-- Mobile menu button -->
          <button
            type="button"
            class="md:hidden p-2 text-slate-700 hover:bg-slate-100 rounded-lg transition-colors"
            id="mobile-menu-button"
            aria-label="メニュー"
          >
            <svg
              class="w-6 h-6"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
        <!-- Mobile menu -->
        <nav class="hidden md:hidden mt-3 pt-3 border-t border-slate-200" id="mobile-menu">
          <div class="flex flex-col gap-1">
            <a href="/sites/" class="px-4 py-2.5 text-sm font-medium text-slate-700 hover:text-blue-600 hover:bg-blue-50 rounded-lg transition-all">
              サイト一覧
            </a>
            <a href="/about/" class="px-4 py-2.5 text-sm font-medium text-slate-700 hover:text-blue-600 hover:bg-blue-50 rounded-lg transition-all">
              このサイトについて
            </a>
          </div>
        </nav>
      </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1">
      <slot />
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-gray-300 mt-auto">
      <div class="max-w-6xl mx-auto px-4 py-8">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <div>
            <h3 class="text-white font-bold mb-4">南関競馬予想サイト口コミ</h3>
            <p class="text-sm">
              南関4場（大井・川崎・船橋・浦和）の予想サイト口コミプラットフォーム。
              首都圏の地方競馬ファンに最適なサイト選びをサポートします。
            </p>
          </div>
          <div>
            <h3 class="text-white font-bold mb-4">南関4場</h3>
            <ul class="space-y-2 text-sm">
              <li class="flex items-center gap-2">
                <span class="text-blue-400">🏇</span>
                <span>大井競馬場（TCK）</span>
              </li>
              <li class="flex items-center gap-2">
                <span class="text-blue-400">🏇</span>
                <span>川崎競馬場</span>
              </li>
              <li class="flex items-center gap-2">
                <span class="text-blue-400">🏇</span>
                <span>船橋競馬場</span>
              </li>
              <li class="flex items-center gap-2">
                <span class="text-blue-400">🏇</span>
                <span>浦和競馬場</span>
              </li>
            </ul>
          </div>
          <div>
            <h3 class="text-white font-bold mb-4">リンク</h3>
            <ul class="space-y-2 text-sm">
              <li><a href="/sites/" class="hover:text-white">サイト一覧</a></li>
              <li><a href="/about/" class="hover:text-white">当サイトについて</a></li>
              <li><a href="/terms/" class="hover:text-white">利用規約</a></li>
              <li><a href="/privacy/" class="hover:text-white">プライバシーポリシー</a></li>
              <li><a href="/contact/" class="hover:text-white">お問い合わせ</a></li>
            </ul>
          </div>
        </div>
        <div class="border-t border-gray-700 mt-8 pt-8 text-center text-sm">
          <p>&copy; {new Date().getFullYear()} 南関競馬予想サイト口コミ. All rights reserved.</p>
        </div>
      </div>
    </footer>

    <script>
      // Mobile menu toggle
      const menuButton = document.getElementById('mobile-menu-button');
      const mobileMenu = document.getElementById('mobile-menu');

      menuButton?.addEventListener('click', () => {
        mobileMenu?.classList.toggle('hidden');
      });
    </script>

    <script is:inline>
      // GA4 Enhanced Event Tracking
      document.addEventListener('DOMContentLoaded', () => {
        // 1. External link tracking (target="_blank")
        document.querySelectorAll('a[target="_blank"]').forEach((link) => {
          link.addEventListener('click', (e) => {
            const href = link.href;
            const text = link.textContent?.trim() || '';

            if (typeof gtag !== 'undefined') {
              gtag('event', 'click', {
                event_category: 'outbound',
                event_label: href,
                link_text: text,
                value: 1
              });
            }
          });
        });

        // 2. Form submission tracking
        document.querySelectorAll('form').forEach((form) => {
          form.addEventListener('submit', (e) => {
            const formId = form.id || 'unknown';
            const formAction = form.action || window.location.pathname;

            if (typeof gtag !== 'undefined') {
              gtag('event', 'form_submit', {
                event_category: 'engagement',
                event_label: formId,
                form_action: formAction,
                value: 1
              });
            }
          });
        });

        // 3. CTA button tracking - nankan-analytics clicks
        document.querySelectorAll('a[href*="nankan-analytics"]').forEach((link) => {
          link.addEventListener('click', (e) => {
            const text = link.textContent?.trim() || '';
            const href = link.href;
            const target = link.target;

            // target="_blank"の場合は遅延不要（新しいタブで開くため）
            if (target === '_blank') {
              if (typeof gtag !== 'undefined') {
                gtag('event', 'cta_click', {
                  event_category: 'conversion',
                  event_label: 'nankan_analytics_cta',
                  link_text: text,
                  link_url: href,
                  value: 1
                });
              }
            } else {
              // 同じタブで開く場合は、イベント送信完了まで遅延
              e.preventDefault();

              if (typeof gtag !== 'undefined') {
                gtag('event', 'cta_click', {
                  event_category: 'conversion',
                  event_label: 'nankan_analytics_cta',
                  link_text: text,
                  link_url: href,
                  value: 1,
                  event_callback: () => {
                    window.location.href = href;
                  }
                });

                // フォールバック: 200ms後に強制的に遷移
                setTimeout(() => {
                  window.location.href = href;
                }, 200);
              } else {
                window.location.href = href;
              }
            }
          });
        });

        // 4. Scroll depth tracking (25%, 50%, 75%, 100%)
        let scrollMarks = { 25: false, 50: false, 75: false, 100: false };

        window.addEventListener('scroll', () => {
          const scrollPercent = Math.round(
            (window.scrollY / (document.documentElement.scrollHeight - window.innerHeight)) * 100
          );

          [25, 50, 75, 100].forEach((mark) => {
            if (scrollPercent >= mark && !scrollMarks[mark]) {
              scrollMarks[mark] = true;

              if (typeof gtag !== 'undefined') {
                gtag('event', 'scroll', {
                  event_category: 'engagement',
                  event_label: `${mark}%`,
                  value: mark
                });
              }
            }
          });
        });

        // 5. "Site visit" button tracking
        document.querySelectorAll('a').forEach((link) => {
          const text = link.textContent?.trim() || '';
          if (text.includes('サイトを見る') || text.includes('公式サイト')) {
            link.addEventListener('click', (e) => {
              const href = link.href;
              const target = link.target;

              // target="_blank"の場合は遅延不要（新しいタブで開くため）
              if (target === '_blank') {
                if (typeof gtag !== 'undefined') {
                  gtag('event', 'site_visit_click', {
                    event_category: 'conversion',
                    event_label: 'site_visit_button',
                    link_url: href,
                    value: 1
                  });
                }
              } else {
                // 同じタブで開く場合は、イベント送信完了まで遅延
                e.preventDefault();

                if (typeof gtag !== 'undefined') {
                  gtag('event', 'site_visit_click', {
                    event_category: 'conversion',
                    event_label: 'site_visit_button',
                    link_url: href,
                    value: 1,
                    event_callback: () => {
                      window.location.href = href;
                    }
                  });

                  // フォールバック: 200ms後に強制的に遷移
                  setTimeout(() => {
                    window.location.href = href;
                  }, 200);
                } else {
                  window.location.href = href;
                }
              }
            });
          }
        });

        // 6. Venue guide page view tracking
        const currentPath = window.location.pathname;
        const venuePages = {
          '/venues/ooi/': '大井',
          '/venues/kawasaki/': '川崎',
          '/venues/funabashi/': '船橋',
          '/venues/urawa/': '浦和'
        };

        if (venuePages[currentPath]) {
          if (typeof gtag !== 'undefined') {
            gtag('event', 'venue_guide_view', {
              event_category: 'engagement',
              event_label: venuePages[currentPath],
              venue_name: venuePages[currentPath],
              page_path: currentPath,
              value: 1
            });
          }
        }

        // 7. FAQ interaction tracking
        document.querySelectorAll('[data-faq-toggle]').forEach((toggle) => {
          toggle.addEventListener('click', (e) => {
            const faqId = toggle.getAttribute('data-faq-id') || 'unknown';
            const faqTitle = toggle.textContent?.trim().substring(0, 50) || '';

            if (typeof gtag !== 'undefined') {
              gtag('event', 'faq_interaction', {
                event_category: 'engagement',
                event_label: faqId,
                faq_title: faqTitle,
                value: 1
              });
            }
          });
        });
      });
    </script>
  </body>
</html>
